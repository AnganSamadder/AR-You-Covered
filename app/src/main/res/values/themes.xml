<?xml version="1.0" encoding="utf-8"?>
<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Theme.ARYouCovered" parent="Theme.AppCompat.DayNight.NoActionBar">
        <!-- Primary brand color. -->
        <item name="colorPrimary">@color/state_farm_red</item>
        <item name="colorPrimaryVariant">@color/state_farm_red_dark</item>
        <item name="colorOnPrimary">@color/white</item>
        <!-- Secondary brand color. -->
        <item name="colorSecondary">@color/state_farm_red_light</item>
        <item name="colorSecondaryVariant">@color/state_farm_red_dark</item>
        <item name="colorOnSecondary">@color/white</item>
        <!-- Status bar color. -->
        <item name="android:statusBarColor" tools:targetApi="l">?attr/colorPrimaryVariant</item>
        <!-- Customize your theme here. -->
    </style>

    <style name="Theme.ARYouCovered.Splash" parent="Theme.AppCompat.DayNight.NoActionBar">
        <!-- Primary brand color. -->
        <item name="colorPrimary">@color/state_farm_red</item>
        <item name="colorPrimaryVariant">@color/state_farm_red_dark</item>
        <item name="colorOnPrimary">@color/white</item>
        <!-- Status bar color. -->
        <item name="android:statusBarColor" tools:targetApi="l">@color/state_farm_red</item>
        <!-- Window background for consistent red background -->
        <item name="android:windowBackground">@color/state_farm_red</item>
        <!-- No splash screen properties - gives us full control -->
    </style>

    <style name="Theme.ARYouCovered.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>

    <style name="Theme.ARYouCovered.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar" />

    <style name="Theme.ARYouCovered.PopupOverlay" parent="ThemeOverlay.AppCompat.Light" />
</resources>